Here is a simple C program to find the largest of two numbers using if–else:

#include <stdio.h>

int main() {
    int num1, num2;

    // Taking input
    printf("Enter first number: ");
    scanf("%d", &num1);

    printf("Enter second number: ");
    scanf("%d", &num2);

    // Comparing numbers
    if (num1 > num2) {
        printf("%d is the largest number\n", num1);
    } else if (num2 > num1) {
        printf("%d is the largest number\n", num2);
    } else {
        printf("Both numbers are equal\n");
    }

    return 0;
}
